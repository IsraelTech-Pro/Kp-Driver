import 'package:flutter/material.dart';

class AppColors {
  // Primary deep teal green (used for branding)
  static const Color primaryColor = Color(0xFF005D57);

  // Accent amber/yellow (used for highlights, buttons)
  static const Color accentColor = Color(0xFFCFA72E);

  // Light background gray
  static const Color backgroundColor = Color(0xFFEBE3CE);

  // Text colors
  static const Color textPrimary = Colors.black87;
  static const Color textSecondary = Colors.black54;

  // Button colors
  static const Color onlineButton = Colors.green;
  static const Color offlineButton = Colors.red;

  // Ride card background
  static const Color cardBackground = Colors.white;

  // Shadows
  static const Color shadowColor = Colors.black26;

  // App bar and nav bar
  static const Color appBarColor = primaryColor;
  static const Color navBarColor = primaryColor;
}
